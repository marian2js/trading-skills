# FMP

Use this path only when the user already indicated `FMP` or chose it from the supported provider list.

Pull only the missing facts needed to finish the preview:

- report date
- session or timing
- EPS estimate
- revenue estimate

Do not turn the task into a data dump. Gather the missing facts, continue the earnings preview, and disclose that `FMP` supplied the schedule or estimate data.
