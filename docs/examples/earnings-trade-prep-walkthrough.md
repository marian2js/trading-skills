# Earnings Trade Prep Walkthrough

Prompt:

> Use `earnings-trade-prep` on NVDA for next week and tell me whether this is a prep-only name, a pre-event trade, or a hold-through candidate.

Expected outcome:

- the workflow runs only the earnings-relevant checks
- the output identifies the key debate and the hold-through logic clearly
- the result ends with a clear event-specific verdict instead of generic commentary

This is a workflow skill. It routes the user through the minimum necessary earnings-related checks rather than doing all analysis itself.
